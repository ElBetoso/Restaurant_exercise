<%= form_with(model: dish_group) do |form| %>
  <% if dish_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dish_group.errors.count, "error") %> prohibited this dish_group from being saved:</h2>

      <ul>
        <% dish_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
